# False rejection TODAY: the token scan sees the `attrs` token in the func
# literal's parameter list AND its body and fires usesAttrs, synthesizing an
# unused Attrs field (the only `attrs` in scope at the call site, `f(nil)`,
# is the parameter, never the implicit bag) — "declared and not used:
# attrs". Stage 2 (freeUseAttrs) pushes a scope for the FuncLit, defines its
# parameter name there, and finds every `attrs` inside the body bound, so
# the two-stage trigger does NOT fire. The generated CounterProps struct
# below carries only the declared `Label` field — NO Attrs field.
-- input.gsx --
package views

component Counter(label string) {
	{{ f := func(attrs []string) int { return len(attrs) } }}
	<div data-label={label} data-n={f(nil)}>x</div>
}
-- invoke --
Counter(CounterProps{Label: "c"})
-- diagnostics.golden --
-- render.golden --
<div data-label="c" data-n="0">x</div>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	_gsxctx "context"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

type CounterProps struct {
	Label string
}

//line input.gsx:3:1
func Counter(_gsxp CounterProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		label := _gsxp.Label
		_gsxgw := _gsxrt.W(_gsxw)
		var _gsxnum [32]byte
//line input.gsx:4:2
		f := func(attrs []string) int { return len(attrs) }
//line input.gsx:5:2
		_gsxgw.S("<div data-label=\"")
		_gsxgw.AttrValue(string(label))
		_gsxgw.S("\" data-n=\"")
		_gsxgw.IntInto(_gsxnum[:], int64(f(nil)))
		_gsxgw.S("\">x</div>")
		return _gsxgw.Err()
	})
}
