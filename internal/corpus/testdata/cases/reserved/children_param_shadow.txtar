# A raw `{{ }}` block is emitted inside the component's render closure, which
# CAPTURES the parameters — so `children := …` shadows the `children` param in a
# nested scope and compiles, matching the emitted code. It is not a reserved-name
# collision. (`ctx`, the closure's OWN parameter, still collides — see
# reserved/ctx_shortvar_rejected.)
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component Page(children gsx.Node) {
	{{ children := "hi" }}
	<div>{children}</div>
}
-- invoke --
Page(nil)
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	_gsxctx "context"
	"github.com/gsxhq/gsx"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

//line input.gsx:5:1
func Page(children gsx.Node) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:6:2
		children := "hi"
//line input.gsx:7:2
		_gsxgw.S("<div>")
//line input.gsx:7:7
		_gsxgw.Text(string(children))
		_gsxgw.S("</div>")
		return _gsxgw.Err()
	})
}
-- render.golden --
<div>hi</div>
