# False rejection TODAY: the token scan sees the `attrs` token in the range
# clause AND the spread and fires usesAttrs, synthesizing an unused Attrs
# field (nothing ever reads the implicit bag local — every `attrs` in the
# body is the range-scoped loop variable) — "declared and not used: attrs".
# Stage 2 (freeUseAttrs) resolves both occurrences against the range
# clause's own binding (env threaded from the ForMarkup clause over its
# markup subtree) and finds no free use, so the two-stage trigger does NOT
# fire. The generated ListProps struct below carries only the declared
# `Label` field — NO Attrs field.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

func bags() []gsx.Attrs {
	return []gsx.Attrs{
		{{Key: "class", Value: "a"}},
		{{Key: "class", Value: "b"}},
	}
}

component List(label string) {
	<p>{label}</p>
	{ for _, attrs := range bags() { <span { attrs... }>i</span> } }
}
-- invoke --
List(ListProps{Label: "items"})
-- diagnostics.golden --
-- render.golden --
<p>items</p><span class="a">i</span><span class="b">i</span>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	_gsxctx "context"
	"github.com/gsxhq/gsx"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

func bags() []gsx.Attrs {
	return []gsx.Attrs{
		{{Key: "class", Value: "a"}},
		{{Key: "class", Value: "b"}},
	}
}

type ListProps struct {
	Label string
}

//line input.gsx:12:1
func List(_gsxp ListProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		label := _gsxp.Label
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:13:2
		_gsxgw.S("<p>")
//line input.gsx:13:5
		_gsxgw.Text(string(label))
		_gsxgw.S("</p>")
//line input.gsx:14:2
		for _, attrs := range bags() {
//line input.gsx:14:35
			_gsxgw.S("<span")
			_gsxgw.ClassMerged(_gsxrt.DefaultClassMerge, attrs.Class())
			_gsxgw.StyleMerged("", attrs.Style())
			_gsxgw.Spread(ctx, attrs, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "src", "xlink:href"}, []string{"background"}, []string{"imagesrcset", "srcset"}, nil, []string{"class", "style"})
			_gsxgw.S(">i</span>")
		}
		return _gsxgw.Err()
	})
}
