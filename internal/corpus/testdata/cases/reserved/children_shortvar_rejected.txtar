# Rejection (spec §14): a body-scope `children :=` collides with the implicit
# children slot local that `{children}` placement brings into scope. Reserved
# regardless of the type mismatch.
-- input.gsx --
package views


import "github.com/gsxhq/gsx"
component Page(children gsx.Node) {
	{{ children := "hi" }}
	<div>{children}</div>
}
-- diagnostics.golden --
6:14: no new variables on left side of :=
6:17: cannot use "hi" (constant of type string) as gsx.Node value in assignment: string does not implement gsx.Node (missing method Render)
