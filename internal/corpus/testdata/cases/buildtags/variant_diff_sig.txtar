-- icon.gsx --
//go:build !never

package views

component Icon(name string) {
	<span>{ name }</span>
}
-- icon_never.gsx --
//go:build never

package views

component Icon(name int) {
	<span>{ name }</span>
}
-- invoke --
-- diagnostics.golden --
5:11: component Icon has different or unresolved semantic signatures across build variants (icon.gsx, icon_never.gsx); parameter names and roles, function types, constraints, and receiver types must be valid and match
5:11: component Icon has different or unresolved semantic signatures across build variants (icon.gsx, icon_never.gsx); parameter names and roles, function types, constraints, and receiver types must be valid and match
