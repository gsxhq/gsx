-- icon.gsx --
//go:build !never

package views

const iconPath = "/active.svg"

component Icon() {
	<img src={ iconPath }/>
}
-- icon_never.gsx --
//go:build never

package views

const iconPath = "/never.svg"

component IconNever() {
	<img src={ iconPath }/>
}
-- invoke --
Icon()
-- diagnostics.golden --
5:7: 	other declaration of iconPath
5:7: iconPath redeclared in this block
-- render.golden --
