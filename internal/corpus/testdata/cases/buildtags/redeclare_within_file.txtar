-- input.gsx --
package views

component Icon(name string) {
	<a/>
}

component Icon(name string) {
	<b/>
}
-- invoke --
-- diagnostics.golden --
3:11: component Icon cannot form a build variant family across input.gsx, input.gsx: input.gsx has no effective Go build constraint
7:11: component Icon cannot form a build variant family across input.gsx, input.gsx: input.gsx has no effective Go build constraint
