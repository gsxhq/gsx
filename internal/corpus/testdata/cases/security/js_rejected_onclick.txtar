-- input.gsx --
package views

component C(x string) {
	<div onclick={x}>y</div>
}
-- diagnostics.golden --
cases/security_js_rejected_onclick/input.gsx: codegen: expr value in JS/event-handler context ("onclick") is unsafe; needs a safe type via `|> js` (not available yet) — use a static value
