-- input.gsx --
package views

component C(x string) { <div data-x={ x |> upper? }>y</div> }
-- diagnostics.golden --
3:37: the `?` try-marker is not supported; gsx auto-unwraps (T, error) values — remove the `?` (handle the error explicitly with `{ if v, err := f(); err != nil { … } }`)
