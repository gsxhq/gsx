-- input.gsx --
package views

component Tags(tags []string, sep string) { <p>{ tags |> join(sep) }</p> }
-- invoke --
Tags([]string{"a","b"}, " / ")
-- diagnostics.golden --
-- render.golden --
<p>a / b</p>
