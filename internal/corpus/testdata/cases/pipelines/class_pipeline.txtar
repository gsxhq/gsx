# a |> pipeline inside a class={ … } composable part lowers identically to text/
# attr interpolation: the part Expr is the pipeline SEED, its Stages lower via the
# SAME lowerPipe (so emit ≡ probe). The `: cond` guard is NEVER piped.
-- input.gsx --
package views

component C(theme string, active bool) {
	<div class={ theme |> upper, "on" : active }>x</div>
}
-- invoke --
C(CProps{Theme: "dark", Active: true})
-- diagnostics.golden --
-- render.golden --
<div class="DARK on">x</div>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
	_gsxstd "github.com/gsxhq/gsx/std"
)

type CProps struct {
	Theme  string
	Active bool
	Attrs  gsx.Attrs
}

//line input.gsx:3:1
func C(_gsxp CProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		theme := _gsxp.Theme
		active := _gsxp.Active
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<div class=\"")
		_gsxgw.Class(gsx.DefaultClassMerge, gsx.Class(_gsxstd.Upper((theme))), gsx.ClassIf("on", active), gsx.Class(_gsxp.Attrs.Class()))
		_gsxgw.S("\"")
		_gsxgw.StyleMerged("", _gsxp.Attrs.Style())
		_gsxgw.Spread(ctx, _gsxp.Attrs.Without("class", "style"))
		_gsxgw.S(">x</div>")
		return _gsxgw.Err()
	})
}
