-- input.gsx --
package views

component Hi(name string) { <p>{ name |> upper? }</p> }
-- diagnostics.golden --
3:32: the `?` try-marker is not supported; gsx auto-unwraps (T, error) values — remove the `?` (handle the error explicitly with `{ if v, err := f(); err != nil { … } }`)
