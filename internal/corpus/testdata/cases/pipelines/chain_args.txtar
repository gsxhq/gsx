# seed-first chained pipeline with args: `s |> trim |> truncate(5)` lowers to
# nested seed-first calls Truncate(Trim((s)), 5). The generated .x.go is pinned
# to lock the emit (which is the SAME lowerPipe output as the probe), proving
# emit ≡ probe for the chained, with-args case.
-- input.gsx --
package views

component Hi(s string) { <p>{ s |> trim |> truncate(5) }</p> }
-- invoke --
Hi(HiProps{S: "  hello world  "})
-- diagnostics.golden --
-- render.golden --
<p>hello</p>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	_gsxctx "context"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxstd "github.com/gsxhq/gsx/std"
	_gsxio "io"
)

type HiProps struct {
	S string
}

//line input.gsx:3:1
func Hi(_gsxp HiProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		s := _gsxp.S
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:3:26
		_gsxgw.S("<p>")
//line input.gsx:3:29
		_gsxgw.Text(string(_gsxstd.Truncate(_gsxstd.Trim((s)), 5)))
		_gsxgw.S("</p>")
		return _gsxgw.Err()
	})
}
