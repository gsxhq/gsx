# {{ }} literal bound to a name with no matching gsx.Attrs field → diagnostic.
-- input.gsx --
package views

component Card(title string) {
	<div>{title}</div>
}

component Page() {
	<Card mystery-attrs={{ "data-x": "1" }}/>
}
-- invoke --
Page()
-- diagnostics.golden --
8:8: Card has no attrs param; unexpected attribute "mystery-attrs"
-- generated.x.go.golden --
-- render.golden --
