# An ordered literal whose name matches no ordinary parameter falls through;
# without a declared `attrs` role, that is a strict missing-attrs diagnostic.
-- input.gsx --
package views

component Card(title string) {
	<div>{title}</div>
}

component Page() {
	<Card mystery-attrs={{ "data-x": "1" }}/>
}
-- invoke --
Page()
-- diagnostics.golden --
8:8: Card has no attrs param; unexpected attribute "mystery-attrs"
-- generated.x.go.golden --
-- render.golden --
