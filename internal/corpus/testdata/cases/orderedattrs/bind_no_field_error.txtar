# {{ }} literal bound to a name with no matching gsx.OrderedAttrs field → diagnostic.
-- input.gsx --
package views

component Card(title string) {
	<div>{title}</div>
}

component Page() {
	<Card mystery-attrs={{ "data-x": "1" }}/>
}
-- invoke --
Page()
-- diagnostics.golden --
8:8: ordered-attrs literal {{ }} on <Card> attribute "mystery-attrs" matches no field of CardProps and cannot fall through to the Attrs bag; declare a gsx.Attrs field to receive it
-- generated.x.go.golden --
-- render.golden --
