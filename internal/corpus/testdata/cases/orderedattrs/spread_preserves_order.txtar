# Spreading a gsx.OrderedAttrs param now emits an error (type removed in Task 1).
# Hand-written literal in the existing ={expr} form — no {{ }} sugar yet.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component Box(bag gsx.OrderedAttrs) {
	<div { bag... }>x</div>
}

component Page() {
	<Box bag={gsx.OrderedAttrs{{Key: "data-signals", Value: "{c:0}"}, {Key: "data-text", Value: "$c"}, {Key: "data-a", Value: "z"}}}/>
}
-- invoke --
Page()
-- diagnostics.golden --
7:10: undefined: gsx.OrderedAttrs
10:28: undefined: gsx.OrderedAttrs
10:41: missing type in composite literal
10:80: missing type in composite literal
10:113: missing type in composite literal
-- render.golden --
-- generated.x.go.golden --
