# Values needing attribute-escaping are safe; unsafe key names are dropped.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component Card(safeAttrs gsx.Attrs) {
	<div { safeAttrs... }></div>
}

component Page() {
	<Card safe-attrs={{ "title": "a\"b", "bad name": "x" }}/>
}
-- invoke --
Page()
-- diagnostics.golden --
10:8: Card has no attrs param; unexpected attribute "safe-attrs"
-- generated.x.go.golden --
-- render.golden --
