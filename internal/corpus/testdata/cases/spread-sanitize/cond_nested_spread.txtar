# A spread NESTED in a conditional attr is a leaf URL sink too (issue #75): a
# `{ if on { { extra... } } }` spread routes through Spread, so a
# javascript: href sanitizes to about:invalid#gsx when the branch is taken, and
# the not-taken branch (`!on`) emits nothing.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component CondSpread(on bool, extra gsx.Attrs) {
	<div>
		<a { if on { { extra... } } }>on</a>
		<a { if !on { { extra... } } }>off</a>
	</div>
}
-- invoke --
CondSpread(CondSpreadProps{On: true, Extra: gsx.Attrs{{Key: "href", Value: "javascript:alert(1)"}}})
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	_gsxctx "context"
	"github.com/gsxhq/gsx"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

type CondSpreadProps struct {
	On    bool
	Extra gsx.Attrs
}

//line input.gsx:5:1
func CondSpread(_gsxp CondSpreadProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		on := _gsxp.On
		extra := _gsxp.Extra
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:6:2
		_gsxgw.S("<div>")
//line input.gsx:7:3
		_gsxgw.S("<a")
		if on {
			_gsxgw.Spread(ctx, extra, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "src", "xlink:href"}, []string{"background"}, []string{"imagesrcset", "srcset"}, nil, nil)
		}
		_gsxgw.S(">on</a>")
//line input.gsx:8:3
		_gsxgw.S("<a")
		if !on {
			_gsxgw.Spread(ctx, extra, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "src", "xlink:href"}, []string{"background"}, []string{"imagesrcset", "srcset"}, nil, nil)
		}
		_gsxgw.S(">off</a></div>")
		return _gsxgw.Err()
	})
}
-- render.golden --
<div><a href="about:invalid#gsx">on</a><a>off</a></div>
