-- input.gsx --
package views

component Card(title string) { <div>{title}</div> }

component Page(t string) { <Card title={t |> upper}/> }
-- diagnostics.golden --
codegen: pipeline/`?` on child-component prop "title" (<Card>) not supported yet
