-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component Panel(header gsx.Node) {
	<div>{header}</div>
}

component Page() {
	<Panel data-x={ <m/> }/>
}
-- diagnostics.golden --
codegen: non-identifier attribute "data-x" on component Panel (attribute fallthrough not supported yet)
