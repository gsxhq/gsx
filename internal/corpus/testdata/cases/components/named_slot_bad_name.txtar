-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component Panel(header gsx.Node) {
	<div>{header}</div>
}

component Page() {
	<Panel data-x={ <m/> }/>
}
-- diagnostics.golden --
10:9: non-identifier attribute "data-x" on component <Panel> (slot names must be valid Go identifiers)
