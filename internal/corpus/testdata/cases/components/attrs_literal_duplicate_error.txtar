# Two attrs={{ }} literals on one element both target the synthesized Attrs
# bag: a positioned ordered-attrs-duplicate error, not a silent last-wins or a
# duplicate struct field.
-- input.gsx --
package views

component Panel() {
	<section { attrs... }>{children}</section>
}

component Page() {
	<Panel attrs={{ "a": "1" }} attrs={{ "b": "2" }}/>
}
-- invoke --
Page()
-- diagnostics.golden --
8:30: duplicate ordered-attrs literal targeting the Attrs bag on <Panel>; combine the pairs into one {{ }} literal
-- generated.x.go.golden --
-- render.golden --
