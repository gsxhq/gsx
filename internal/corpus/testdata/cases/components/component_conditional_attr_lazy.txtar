# COVERAGE / REGRESSION-PIN (gap #2): a conditional attribute branch is emitted as
# a THUNK, so its expressions are evaluated ONLY when the branch is taken. Here the
# branch derefs `u.Name`; invoked with u == nil, the untaken thunk must NOT run, so
# there is no panic and no data-name attribute. Pins the lazy-eval fix.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

type User struct {
	Name string
}

component Card(title gsx.Node) { <div class="card">{title}</div> }

component Page(u *User) {
	<Card title="Hi" { if u != nil { data-name={ u.Name } } } />
}
-- invoke --
Page(PageProps{U: nil})
-- diagnostics.golden --
-- render.golden --
<div class="card">Hi</div>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
)

type User struct {
	Name string
}

type CardProps struct {
	Title gsx.Node
	Attrs gsx.Attrs
}

func Card(_gsxp CardProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		title := _gsxp.Title
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:9:34
		_gsxgw.S("<div class=\"")
		_gsxgw.Class(gsx.Class("card"), gsx.Class(_gsxp.Attrs.Class()))
		_gsxgw.S("\"")
		_gsxgw.StyleMerged("", _gsxp.Attrs.Style())
		_gsxgw.Spread(ctx, _gsxp.Attrs.Without("class", "style"))
		_gsxgw.S(">")
//line input.gsx:9:52
		_gsxgw.Node(ctx, title)
		_gsxgw.S("</div>")
		return _gsxgw.Err()
	})
}

type PageProps struct {
	U *User
}

func Page(_gsxp PageProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		u := _gsxp.U
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:12:2
		_gsxgw.Node(ctx, Card(CardProps{Title: gsx.Text("Hi"), Attrs: gsx.Attrs{}.Merge(gsx.AttrsCond(u != nil, func() gsx.Attrs { return gsx.Attrs{"data-name": u.Name} }, nil))}))
		return _gsxgw.Err()
	})
}
