# COVERAGE / REGRESSION-PIN (gap #2): a conditional attribute branch is emitted as
# a THUNK, so its expressions are evaluated ONLY when the branch is taken. Here the
# branch derefs `u.Name`; invoked with u == nil, the untaken thunk must NOT run, so
# there is no panic and no data-name attribute. Pins the lazy-eval fix.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

type User struct {
	Name string
}

component Card(title gsx.Node, attrs gsx.Attrs) { <div class="card" { attrs... }>{title}</div> }

component Page(u *User) {
	<Card title="Hi" { if u != nil { data-name={ u.Name } } } />
}
-- invoke --
Page(nil)
-- diagnostics.golden --
12:2: attribute "title" cannot be passed to github.com/gsxhq/gsx.Node: zero.go:2:18: cannot use "Hi" (constant of type string) as _gsxwant value in variable declaration: string does not implement gsx.Node (missing method Render)
-- render.golden --
-- generated.x.go.golden --
