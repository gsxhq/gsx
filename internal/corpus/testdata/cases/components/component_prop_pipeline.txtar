# TARGET (gap #2): a |> pipeline on a child-component prop value lowers like every
# other interpolation context.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component Card(title gsx.Node) { <div class="card">{title}</div> }

component Page(name string) {
	<Card title={ name |> upper } />
}
-- invoke --
Page("ada")
-- diagnostics.golden --
8:2: attribute "title" cannot be passed to github.com/gsxhq/gsx.Node: zero.go:2:18: cannot use _gsxvalue (variable of type string) as _gsxwant value in variable declaration: string does not implement gsx.Node (missing method Render)
-- render.golden --
-- generated.x.go.golden --
