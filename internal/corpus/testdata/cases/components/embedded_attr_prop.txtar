# BARE (unbraced) js`…`/css`…` embedded-language literals forward to a
# component's Attrs bag — JSX-style directive forwarding (e.g. x-model=js`…`).
# The bag value is a trusted _gsxrt.RawJS/_gsxrt.RawCSS (not a plain string), so
# provenance survives the child's re-interpolation on { attrs... } spread; the
# rendered output is unchanged because toStr prints RawJS/RawCSS as source. A
# bare `…` value stays a plain Go raw string (data-tip here). Bare literals
# ALWAYS fall through to the bag regardless of name; to bind a declared prop use
# the braced { js`…` }/{ css`…` } form (embedded_prop_binding.txtar), a string,
# or an f-literal.
-- input.gsx --
package views


import "github.com/gsxhq/gsx"
component Field(label string, attrs gsx.Attrs) { <input aria-label={label} { attrs... }/> }

component Page() {
	<Field
		label="Category"
		x-model=js`pdcaCategory`
		data-tip=`plain text`
		x-init=css`color:red`
	/>
}
-- invoke --
Page()
-- diagnostics.golden --
-- render.golden --
<input aria-label="Category" x-model="pdcaCategory" data-tip="plain text" x-init="color:red"/>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	_gsxctx "context"
	"github.com/gsxhq/gsx"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

//line input.gsx:5:1
func Field(label string, attrs gsx.Attrs) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:5:50
		_gsxgw.S("<input")
		if !attrs.Has("aria-label") {
			_gsxgw.S(" aria-label=\"")
			_gsxgw.AttrValue(string(label))
			_gsxgw.S("\"")
		}
		_gsxgw.ClassMerged(_gsxrt.DefaultClassMerge, attrs.Class())
		_gsxgw.StyleMerged("", attrs.Style())
		_gsxgw.Spread(ctx, attrs, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "xlink:href"}, []string{"background", "src"}, []string{"imagesrcset", "srcset"}, nil, []string{"class", "style"})
		_gsxgw.S("/>")
		return _gsxgw.Err()
	})
}

//line input.gsx:7:1
func Page() _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:8:2
		_gsxgw.Node(ctx, Field("Category", _gsxrt.ConcatAttrs(_gsxrt.Attrs{{Key: "x-model", Value: "pdcaCategory"}}, _gsxrt.Attrs{{Key: "data-tip", Value: `plain text`}}, _gsxrt.Attrs{{Key: "x-init", Value: "color:red"}})))
		return _gsxgw.Err()
	})
}
