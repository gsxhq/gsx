# COVERAGE (gap #2): a |> pipeline on a FALLTHROUGH attr (data-x is not a Card prop)
# lowers like a prop pipeline but lands as a bag VALUE (not gsx.Val). Pins render +
# that the _gsxstd filter package is imported.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component Card(title gsx.Node, attrs gsx.Attrs) { <div class="card" { attrs... }>{title}</div> }

component Page(name string) {
	<Card title="Hi" data-x={ name |> upper } />
}
-- invoke --
Page("ada")
-- diagnostics.golden --
8:2: attribute "title" cannot be passed to github.com/gsxhq/gsx.Node: zero.go:2:18: cannot use "Hi" (constant of type string) as _gsxwant value in variable declaration: string does not implement gsx.Node (missing method Render)
-- render.golden --
-- generated.x.go.golden --
