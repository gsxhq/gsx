# A BRACED js`…`/css`…` embedded literal whose name is NOT a declared prop falls
# through to the child's Attrs bag as a trusted _gsxrt.RawJS/_gsxrt.RawCSS value
# (never a plain string) — proving that braced binds ONLY a declared name; an
# undeclared braced literal routes to the bag exactly like the bare form
# (embedded_attr_prop.txtar). The provenance survives the re-interpolation the
# child performs on { attrs... } spread; rendered output is unchanged vs the
# plain-string era because toStr prints RawJS/RawCSS as source through AttrValue.
# (Hole-bearing bag forwarding stays unsupported — see embedded_attr_rejected.)
-- input.gsx --
package demo

component Field(label string) { <button aria-label={label} { attrs... }>x</button> }

component Page() {
	<Field
		label="Go"
		@click={ js`toggle()` }
		data-style={ css`color:red` }
	/>
}
-- invoke --
Page()
-- diagnostics.golden --
-- render.golden --
<button aria-label="Go" @click="toggle()" data-style="color:red">x</button>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package demo

import (
	_gsxctx "context"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

type FieldProps struct {
	Label string
	Attrs _gsxrt.Attrs
}

//line input.gsx:3:1
func Field(_gsxp FieldProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		label := _gsxp.Label
		attrs := _gsxp.Attrs
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:3:33
		_gsxgw.S("<button")
		if !attrs.Has("aria-label") {
			_gsxgw.S(" aria-label=\"")
			_gsxgw.AttrValue(string(label))
			_gsxgw.S("\"")
		}
		_gsxgw.ClassMerged(_gsxrt.DefaultClassMerge, attrs.Class())
		_gsxgw.StyleMerged("", attrs.Style())
		_gsxgw.Spread(ctx, attrs, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "src", "xlink:href"}, []string{"background"}, []string{"imagesrcset", "srcset"}, nil, []string{"class", "style"})
		_gsxgw.S(">x</button>")
		return _gsxgw.Err()
	})
}

//line input.gsx:5:1
func Page() _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:6:2
		_gsxgw.Node(ctx, Field(FieldProps{Label: "Go", Attrs: _gsxrt.Attrs{{Key: "@click", Value: _gsxrt.RawJS("toggle()")}, {Key: "data-style", Value: _gsxrt.RawCSS("color:red")}}}))
		return _gsxgw.Err()
	})
}
