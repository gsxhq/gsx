-- input.gsx --
package views

var state bool

component Card() { <div>{children}</div> }

component Page() {
	<Card class={ setState(), if state { "on" } else { "off" } }>x</Card>
}

func setState() string {
	state = true
	return "base"
}
-- invoke --
Page()
-- render.golden --
-- diagnostics.golden --
8:8: attribute on <Card> matches no declared prop of component Card, and its body does not reference `attrs` (no fallthrough bag); reference `attrs` in <Card>'s body to accept forwarded attributes
-- generated.x.go.golden --
