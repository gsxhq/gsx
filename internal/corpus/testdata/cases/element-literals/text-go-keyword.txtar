# Context: an element literal whose prose body contains the bare word
# "component" — a gsx keyword, but here just English text. The file-level scan
# that locates top-level `component` declarations tokenizes the Go gaps with
# go/scanner; before the fix it lexed element prose as Go, matched this word as
# an IDENT at brace depth 0, and split the file mid-<p> (yielding "unexpected
# EOF, expected </p>" then "expected component name"). Pins that the word
# survives as text in BOTH a multi-line var literal and a function-local
# literal (brace depth > 0), and that the real `component` decl below is still
# found. `gsx.Node` needs a real import here, as in func-local.txtar.
-- input.gsx --
package demo

import "github.com/gsxhq/gsx"

var X = (
	<div>
		<h1>Test</h1>
		<p>This is a test component.</p>
	</div>
)

func local() gsx.Node {
	n := <em>another component</em>
	return n
}

component Uses() {
	<section>{ X }{ local() }</section>
}
-- invoke --
Uses()
-- diagnostics.golden --
-- render.golden --
<section><div><h1>Test</h1><p>This is a test component.</p></div><em>another component</em></section>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package demo

import (
	_gsxctx "context"
	"github.com/gsxhq/gsx"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

/*line input.gsx:5:1*/
var X = _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
	_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:6:2
	_gsxgw.S("<div>")
//line input.gsx:7:3
	_gsxgw.S("<h1>Test</h1>")
//line input.gsx:8:3
	_gsxgw.S("<p>This is a test component.</p></div>")
	return _gsxgw.Err()
}) /*line input.gsx:10:2*/

func local() gsx.Node {
	n := _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:13:7
		_gsxgw.S("<em>another component</em>")
		return _gsxgw.Err()
	}) /*line input.gsx:13:33*/
	return n
}

//line input.gsx:17:1
func Uses() _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:18:2
		_gsxgw.S("<section>")
//line input.gsx:18:11
		_gsxgw.Node(ctx, X)
//line input.gsx:18:16
		_gsxgw.Node(ctx, local())
		_gsxgw.S("</section>")
		return _gsxgw.Err()
	})
}
