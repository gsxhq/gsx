# URL image sink, Form A: a []byte hole immediately following a `;base64,`
# marker in a data: URL literal is base64-encoded (not raw bytes) before the
# whole value is assembled and passed through _gsxgw.URLImage.
-- input.gsx --
package p

component Avatar(png []byte) {
	<img src=`data:image/png;base64,@{png}` alt="avatar" />
}
-- invoke --
Avatar(AvatarProps{Png: []byte("hi")})
-- diagnostics.golden --
-- render.golden --
<img src="data:image/png;base64,aGk=" alt="avatar"/>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package p

import (
	"context"
	"encoding/base64"
	"io"

	"github.com/gsxhq/gsx"
)

type AvatarProps struct {
	Png []byte
}

//line input.gsx:3:1
func Avatar(_gsxp AvatarProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		png := _gsxp.Png
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<img src=\"")
		_gsxgw.URLImage("data:image/png;base64," + base64.StdEncoding.EncodeToString(png))
		_gsxgw.S("\" alt=\"avatar\"/>")
		return _gsxgw.Err()
	})
}
