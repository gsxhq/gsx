# URL image sink, Form A with a string hole: unlike the []byte case
# (data_image_bytes.txtar), a string hole after `;base64,` is NOT re-encoded —
# the author already supplies base64 text, so it passes through as a plain
# string conversion before the whole value is assembled and sanitized by
# _gsxgw.URLImage.
-- input.gsx --
package p

component Avatar(b64 string) {
	<img src=`data:image/png;base64,@{b64}` alt="a" />
}
-- invoke --
Avatar(AvatarProps{B64: "aGVsbG8="})
-- diagnostics.golden --
-- render.golden --
<img src="data:image/png;base64,aGVsbG8=" alt="a"/>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package p

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
)

type AvatarProps struct {
	B64 string
}

//line input.gsx:3:1
func Avatar(_gsxp AvatarProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		b64 := _gsxp.B64
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<img src=\"")
		_gsxgw.URLImage("data:image/png;base64," + string(b64))
		_gsxgw.S("\" alt=\"a\"/>")
		return _gsxgw.Err()
	})
}
