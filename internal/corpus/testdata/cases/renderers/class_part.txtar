# A composable class={ val } part whose value is the REGISTERED type
# pg.Text: composedParts' plain-part branch had NO type handling at all
# before this task — gsx.Class(s string) requires expr to literally BE a Go
# string (see class/value_part_nonstring_rejected.txtar's note: a non-string
# part is a hard Go compile error, not a gsx diagnostic), so a registered
# non-string-typed part previously failed to compile. The renderer now
# converts it to a string BEFORE the .Class(...) call, exactly like every
# other render boundary.
-- gsx.toml --
[renderers]
"./pg.Text" = "./rend.PgText"
-- pg/pg.go --
package pg

type Text struct {
	String string
	Valid  bool
}
-- rend/rend.go --
package rend

import "corpustest/cases/renderers_class_part/pg"

func PgText(t pg.Text) string {
	if !t.Valid {
		return ""
	}
	return t.String
}
-- input.gsx --
package views

import "corpustest/cases/renderers_class_part/pg"

func mkText(s string, valid bool) pg.Text { return pg.Text{String: s, Valid: valid} }

component Tag(val pg.Text) {
	<div class={ val }>x</div>
}
-- invoke --
Tag(TagProps{Val: mkText("btn primary", true)})
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"corpustest/cases/renderers_class_part/pg"

	_gsxctx "context"
	_gsxf0 "corpustest/cases/renderers_class_part/rend"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

func mkText(s string, valid bool) pg.Text { return pg.Text{String: s, Valid: valid} }

type TagProps struct {
	Val pg.Text
}

//line input.gsx:7:1
func Tag(_gsxp TagProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		val := _gsxp.Val
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:8:2
		_gsxgw.S("<div class=\"")
		_gsxgw.Class(_gsxrt.DefaultClassMerge, _gsxrt.Class(_gsxf0.PgText((val))))
		_gsxgw.S("\">x</div>")
		return _gsxgw.Err()
	})
}
-- render.golden --
<div class="btn primary">x</div>
