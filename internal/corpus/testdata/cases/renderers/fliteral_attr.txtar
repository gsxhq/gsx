# A URL-context attribute f-literal href=`/x/@{val}` with a hole of the
# REGISTERED type pg.Text: the per-hole assembly (embeddedTextValueExpr ->
# embeddedValueExpr -> holeStringExpr) applies the renderer to the hole
# BEFORE the whole literal is concatenated and sanitized once by gw.URL —
# this is the Task 5 hand-off site (holeStringExpr wired but uncased). A
# non-URL, no-pipe attr f-literal (e.g. title=`v=@{val}`) instead routes
# per-hole through emitTextAttrInterp (already covered by attr_basic.txtar),
# so this case deliberately uses a URL-context attribute to exercise
# holeStringExpr specifically, not that already-covered sibling path.
-- gsx.toml --
[renderers]
"./pg.Text" = "./rend.PgText"
-- pg/pg.go --
package pg

type Text struct {
	String string
	Valid  bool
}
-- rend/rend.go --
package rend

import "corpustest/cases/renderers_fliteral_attr/pg"

func PgText(t pg.Text) string {
	if !t.Valid {
		return ""
	}
	return t.String
}
-- input.gsx --
package views

import "corpustest/cases/renderers_fliteral_attr/pg"

func mkText(s string, valid bool) pg.Text { return pg.Text{String: s, Valid: valid} }

component Tag(val pg.Text) {
	<a href=f`/x/@{val}`>ok</a>
}
-- invoke --
Tag(TagProps{Val: mkText("hi there", true)})
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"corpustest/cases/renderers_fliteral_attr/pg"

	_gsxctx "context"
	_gsxf32 "corpustest/cases/renderers_fliteral_attr/rend"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

func mkText(s string, valid bool) pg.Text { return pg.Text{String: s, Valid: valid} }

type TagProps struct {
	Val pg.Text
}

//line input.gsx:7:1
func Tag(_gsxp TagProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		val := _gsxp.Val
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:8:2
		_gsxgw.S("<a href=\"")
		_gsxgw.URL("/x/" + string(_gsxf32.PgText((val))))
		_gsxgw.S("\">ok</a>")
		return _gsxgw.Err()
	})
}
-- render.golden --
<a href="/x/hi there">ok</a>
