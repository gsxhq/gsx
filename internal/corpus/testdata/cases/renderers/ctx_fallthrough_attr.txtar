# An UNMATCHED ExprAttr on a child-component invocation whose value's
# registered renderer wants ctx — the ctx-taking sibling of
# fallthrough_attr.txtar: `data-x={ val }` lands in the fallthrough Attrs-bag
# literal (childPropsLiteral's bag splice), a STATIC boundary, so ctx
# injection + the (R, error) hoist must both apply BEFORE the value enters
# the bag as `any`. Pins `_gsxvN, _gsxerr := rend.PgText(ctx, (val)); if
# _gsxerr != nil { return _gsxerr }` — single-value errReturn (the enclosing
# render closure, not a thunk; the thunk case is ctx_cond_attr.txtar).
-- gsx.toml --
[renderers]
"./pg.Text" = "./rend.PgText"
-- pg/pg.go --
package pg

type Text struct {
	String string
	Valid  bool
}
-- rend/rend.go --
package rend

import (
	"context"
	"errors"

	"corpustest/cases/renderers_ctx_fallthrough_attr/pg"
)

func PgText(ctx context.Context, t pg.Text) (string, error) {
	if ctx == nil {
		return "", errors.New("pgtext: no ctx")
	}
	if !t.Valid {
		return "", errors.New("pgtext: invalid value")
	}
	return "RENDERED:" + t.String, nil
}
-- input.gsx --
package views

import (
	"github.com/gsxhq/gsx"

	"corpustest/cases/renderers_ctx_fallthrough_attr/pg"
)

func mkText(s string, valid bool) pg.Text { return pg.Text{String: s, Valid: valid} }

component Card(title gsx.Node) { <div { attrs... }>{title}</div> }

component Page(val pg.Text) {
	<Card title="Hi" data-x={ val } />
}
-- invoke --
Page(PageProps{Val: mkText("hi", true)})
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"corpustest/cases/renderers_ctx_fallthrough_attr/pg"

	_gsxctx "context"
	_gsxf18 "corpustest/cases/renderers_ctx_fallthrough_attr/rend"
	"github.com/gsxhq/gsx"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

func mkText(s string, valid bool) pg.Text { return pg.Text{String: s, Valid: valid} }

type CardProps struct {
	Title gsx.Node
	Attrs _gsxrt.Attrs
}

//line input.gsx:11:1
func Card(_gsxp CardProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		title := _gsxp.Title
		attrs := _gsxp.Attrs
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:11:34
		_gsxgw.S("<div")
		_gsxgw.ClassMerged(_gsxrt.DefaultClassMerge, attrs.Class())
		_gsxgw.StyleMerged("", attrs.Style())
		_gsxgw.Spread(ctx, attrs, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "src", "xlink:href"}, []string{"background"}, []string{"imagesrcset", "srcset"}, nil, []string{"class", "style"})
		_gsxgw.S(">")
//line input.gsx:11:52
		_gsxgw.Node(ctx, title)
		_gsxgw.S("</div>")
		return _gsxgw.Err()
	})
}

type PageProps struct {
	Val pg.Text
}

//line input.gsx:13:1
func Page(_gsxp PageProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		val := _gsxp.Val
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:14:2
		_gsxv0, _gsxerr := _gsxf18.PgText(ctx, (val))
		if _gsxerr != nil {
			return _gsxerr
		}
		_gsxgw.Node(ctx, Card(CardProps{Title: _gsxrt.Text("Hi"), Attrs: _gsxrt.Attrs{{Key: "data-x", Value: _gsxv0}}}))
		return _gsxgw.Err()
	})
}
-- render.golden --
<div data-x="RENDERED:hi">Hi</div>
