# A text hole whose registered renderer takes the ambient ctx as its first
# param — the ctx-taking sibling of text_basic.txtar. Pins that applyRenderer
# prepends pipeCtxIdent ("ctx") before the subject expr: the renderer itself
# proves ctx is real (non-nil) by returning "NO-CTX" if it weren't threaded
# through, so render.golden showing the actual value (not "NO-CTX") is the
# proof that emission passes the render closure's ctx, not a zero value.
-- gsx.toml --
[renderers]
"./pg.Text" = "./rend.PgText"
-- pg/pg.go --
package pg

type Text struct {
	String string
	Valid  bool
}
-- rend/rend.go --
package rend

import (
	"context"

	"corpustest/cases/renderers_ctx_text/pg"
)

func PgText(ctx context.Context, t pg.Text) string {
	if ctx == nil {
		return "NO-CTX"
	}
	if !t.Valid {
		return ""
	}
	return t.String
}
-- input.gsx --
package views

import "corpustest/cases/renderers_ctx_text/pg"

func mkText(s string, valid bool) pg.Text { return pg.Text{String: s, Valid: valid} }

component Tag(val pg.Text) {
	<div>{ val }</div>
}
-- invoke --
Tag(TagProps{Val: mkText("<b>hi</b>", true)})
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"corpustest/cases/renderers_ctx_text/pg"

	_gsxctx "context"
	_gsxf16 "corpustest/cases/renderers_ctx_text/rend"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

func mkText(s string, valid bool) pg.Text { return pg.Text{String: s, Valid: valid} }

type TagProps struct {
	Val pg.Text
}

//line input.gsx:7:1
func Tag(_gsxp TagProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		val := _gsxp.Val
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:8:2
		_gsxgw.S("<div>")
//line input.gsx:8:7
		_gsxgw.Text(string(_gsxf16.PgText(ctx, (val))))
		_gsxgw.S("</div>")
		return _gsxgw.Err()
	})
}
-- render.golden --
<div>&lt;b&gt;hi&lt;/b&gt;</div>
