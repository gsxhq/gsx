# A hand-written arity-1 function exposes its exact static signature. Its
# author-owned struct is passed through the ordinary parameter `p` as a whole.
-- badge.go --
package views

import "github.com/gsxhq/gsx"

type BadgeProps struct{ Count int }

func Badge(p BadgeProps) gsx.Node { return gsx.Raw("x") }
-- input.gsx --
package views

component Page(count int) { <Badge p={BadgeProps{Count: count}}/> }
-- diagnostics.golden --
