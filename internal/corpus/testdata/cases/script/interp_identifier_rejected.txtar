-- input.gsx --
package views

component Page() {
	<script>
		let @{ name } = 1;
	</script>
}
-- diagnostics.golden --
5:8: jsx: @{ } here is not a safe JavaScript value position (it looks like an identifier/binding); wrap the value where it is used as a value, or use a data attribute
