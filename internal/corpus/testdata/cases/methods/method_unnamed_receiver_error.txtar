-- input.gsx --
package views

type UsersPage struct{ Title string }

component (UsersPage) X() {
	<p>x</p>
}
-- diagnostics.golden --
codegen: method component receiver must be named, e.g. (p T) — got "(UsersPage)"
