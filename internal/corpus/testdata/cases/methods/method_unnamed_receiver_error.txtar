-- input.gsx --
package views

type UsersPage struct{ Title string }

component (UsersPage) X() {
	<p>x</p>
}
-- diagnostics.golden --
5:1: method component receiver must be named, e.g. (p T) — got "(UsersPage)"
