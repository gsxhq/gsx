# W2: an error-returning pipe stage in a js`` hole INSIDE a component's { }
# interp — the render closure has a hoist channel (genInterp's Interp.Embedded
# split, canHoist=true), so the hole hoists `_gsxvN, _gsxerr := …` exactly
# like the identical f`` hole does (parity rule: wherever f`` can hoist,
# js``/css`` can too). Same shape as ctx_filter_in_closure.txtar (a js`` literal
# wrapped in a sibling .go call inside a body `{ }`), with the ctx-taking
# filter swapped for an error-returning one. Success path.
-- gsx.toml --
filter_packages = ["./filters"]
-- filters/filters.go --
package filters

import "errors"

// Parse fails on empty input.
func Parse(s string) (string, error) {
	if s == "" {
		return "", errors.New("parse: empty input")
	}
	return s, nil
}
-- input.gsx --
package demo

import "github.com/gsxhq/gsx"

func take(j gsx.RawJS) gsx.Node { return gsx.Raw(string(j)) }

component Page(csv string) {
	<div>{ take(js`load(@{csv |> parse})`) }</div>
}
-- invoke --
Page("a,b")
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package demo

import (
	_gsxctx "context"
	_gsxf0 "corpustest/cases/goexpr-js-literal_err_pipe_in_closure/filters"
	"github.com/gsxhq/gsx"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

//line input.gsx:5:1
func take(j gsx.RawJS) gsx.Node { return gsx.Raw(string(j)) }

//line input.gsx:7:1
func Page(csv string) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:8:2
		_gsxgw.S("<div>")
//line input.gsx:8:7
		_gsxv0, _gsxerr := _gsxf0.Parse((csv))
		if _gsxerr != nil {
			return _gsxerr
		}
		_gsxv1 := _gsxrt.EscapeJSVal(_gsxv0)
		_gsxgw.Node(ctx, take(_gsxrt.RawJS("load("+_gsxv1+")")))
		_gsxgw.S("</div>")
		return _gsxgw.Err()
	})
}
-- render.golden --
<div>load("a,b")</div>
