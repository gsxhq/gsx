# Regression: a non-binding type switch as a value-form tag. The tag
# `v.(type)` and type-name case lists are only legal inside a switch
# statement — `_ = (v.(type))` / `_ = (string)` are not expressions — so the
# skeleton must reference them in statement position.
-- input.gsx --
package views

component C(v any) {
	<span class={ switch v.(type) {
	case string: "str"
	default: "other"
	} }>x</span>
}
-- invoke --
C(CProps{V: "hi"})
-- diagnostics.golden --
-- render.golden --
<span class="str">x</span>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
)

type CProps struct {
	V any
}

//line input.gsx:3:1
func C(_gsxp CProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		v := _gsxp.V
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<span")
		var _gsxv0 string
		switch v.(type) {
		case string:
			_gsxv0 = "str"
		default:
			_gsxv0 = "other"
		}
		_gsxgw.S(" class=\"")
		_gsxgw.Class(gsx.DefaultClassMerge, gsx.Class(_gsxv0))
		_gsxgw.S("\">x</span>")
		return _gsxgw.Err()
	})
}
