# Regression: `case nil` in a value-form switch (nilable tag) must generate.
# The type-resolution skeleton used to reference case exprs as `_ = (expr)`,
# and `_ = (nil)` is illegal Go ("use of untyped nil"); control expressions
# must be referenced in statement position instead.
-- input.gsx --
package views

component C(p *int) {
	<span class={ "base", switch p {
	case nil: "none"
	default: "some"
	} }>x</span>
}
-- invoke --
C(CProps{})
-- diagnostics.golden --
-- render.golden --
<span class="base none">x</span>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
)

type CProps struct {
	P *int
}

//line input.gsx:3:1
func C(_gsxp CProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		p := _gsxp.P
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<span")
		_gsxv0 := "base"
		var _gsxv1 string
		switch p {
		case nil:
			_gsxv1 = "none"
		default:
			_gsxv1 = "some"
		}
		_gsxgw.S(" class=\"")
		_gsxgw.Class(gsx.DefaultClassMerge, gsx.Class(_gsxv0), gsx.Class(_gsxv1))
		_gsxgw.S("\">x</span>")
		return _gsxgw.Err()
	})
}
