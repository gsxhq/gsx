-- gsx.toml --
class_merger = "corpustest/cases/class_merger_composable/mrg.Keep"
-- mrg/mrg.go --
package mrg

// Keep is a deterministic, NON-default merger: it joins raw class strings with
// "|" and keeps duplicates, so the golden visibly differs from DefaultClassMerge.
func Keep(tokens []string) string {
	out := ""
	for i, t := range tokens {
		if i > 0 {
			out += "|"
		}
		out += t
	}
	return out
}
-- input.gsx --
package views

component C(on bool) { <div class={ "btn", "btn-on": on }>y</div> }
-- invoke --
C(CProps{On: true})
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"context"
	"io"

	_gsxcm "corpustest/cases/class_merger_composable/mrg"
	"github.com/gsxhq/gsx"
)

type CProps struct {
	On    bool
	Attrs gsx.Attrs
}

//line input.gsx:3:1
func C(_gsxp CProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		on := _gsxp.On
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:3:24
		_gsxgw.S("<div class=\"")
		_gsxgw.Class(_gsxcm.Keep, gsx.Class("btn"), gsx.ClassIf("btn-on", on), gsx.Class(_gsxp.Attrs.Class()))
		_gsxgw.S("\"")
		_gsxgw.StyleMerged("", _gsxp.Attrs.Style())
		_gsxgw.Spread(ctx, _gsxp.Attrs.Without("class", "style"))
		_gsxgw.S(">y</div>")
		return _gsxgw.Err()
	})
}
-- render.golden --
<div class="btn|btn-on">y</div>
