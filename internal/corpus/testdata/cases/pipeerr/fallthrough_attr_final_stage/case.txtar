# A FINAL-stage (R, error) filter used as a COMPONENT FALLTHROUGH attr — the one
# attr context that lacked final-stage unwrap. `hx-get` is undeclared on <Child>,
# so it lands in the caller-built Attrs bag; the pipeline's terminal (string,
# error) tuple must be hoisted (v, err := call; if err != nil { return err })
# BEFORE the bag literal, exactly like a declared prop or a direct element attr.
# (A MID-stage error already worked via emitPipeWrap; this pins the final stage.)
-- gsx.toml --
filterPackages = ["./filters"]
-- filters/filters.go --
package filters

import (
	"context"
	"errors"
)

// Url prefixes "/" and takes the ambient ctx; fails on empty input.
func Url(ctx context.Context, s string) (string, error) {
	if s == "" {
		return "", errors.New("url: empty input")
	}
	return "/" + s, nil
}
-- input.gsx --
package views

component Child(variant string) {
	<a class={ "a", variant } { attrs... }>{children}</a>
}

component Page(target string) {
	<Child variant="x" hx-get={ target |> url }>Go</Child>
}
-- invoke --
Page(PageProps{Target: "home"})
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	_gsxctx "context"
	_gsxf0 "corpustest/cases/pipeerr_fallthrough_attr_final_stage_case/filters"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

type ChildProps struct {
	Variant  string
	Children _gsxrt.Node
	Attrs    _gsxrt.Attrs
}

//line input.gsx:3:1
func Child(_gsxp ChildProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		variant := _gsxp.Variant
		children := _gsxp.Children
		attrs := _gsxp.Attrs
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<a class=\"")
		_gsxgw.Class(_gsxrt.DefaultClassMerge, _gsxrt.Class("a"), _gsxrt.Class(variant), _gsxrt.Class(attrs.Class()))
		_gsxgw.S("\"")
		_gsxgw.StyleMerged("", attrs.Style())
		_gsxgw.Spread(ctx, attrs, []string{"action", "cite", "data", "formaction", "href", "manifest", "ping", "poster", "src", "xlink:href"}, []string{"background"}, []string{"imagesrcset", "srcset"}, nil, []string{"class", "style"})
		_gsxgw.S(">")
//line input.gsx:4:41
		_gsxgw.Node(ctx, children)
		_gsxgw.S("</a>")
		return _gsxgw.Err()
	})
}

type PageProps struct {
	Target string
}

//line input.gsx:7:1
func Page(_gsxp PageProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		target := _gsxp.Target
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:8:2
		_gsxv0, _gsxerr := _gsxf0.Url(ctx, (target))
		if _gsxerr != nil {
			return _gsxerr
		}
		_gsxgw.Node(ctx, Child(ChildProps{Variant: "x", Children: _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
			_gsxgw := _gsxrt.W(_gsxw)
			_gsxgw.S("Go")
			return _gsxgw.Err()
		}), Attrs: _gsxrt.Attrs{{Key: "hx-get", Value: _gsxv0}}}))
		return _gsxgw.Err()
	})
}
-- render.golden --
<a class="a x" hx-get="/home">Go</a>
