# Context: fragment literal (`<>…</>`) as a top-level `var` initializer —
# the emit≡probe gate for Task 4. `pair` is inferred as gsx.Node from the
# fragment's two sibling elements; a component later interpolates it like
# any other node value. Confirms a fragment part lowers via the SAME shared
# emitNodeValue path an element literal uses (`gsx.Func(func(ctx, w){ … })`),
# just keyed on the fragment's Children list instead of a single element.
-- input.gsx --
package views

var pair = <><span>one</span><span>two</span></>

component Host() {
	{ pair }
}
-- invoke --
Host()
-- diagnostics.golden --
-- render.golden --
<span>one</span><span>two</span>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
)

var pair = gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
	_gsxgw := gsx.W(_gsxw)
//line input.gsx:3:14
	_gsxgw.S("<span>one</span>")
//line input.gsx:3:30
	_gsxgw.S("<span>two</span>")
	return _gsxgw.Err()
})

//line input.gsx:5:1
func Host() gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:6:2
		_gsxgw.Node(ctx, pair)
		return _gsxgw.Err()
	})
}
