# Context: fragment child interpolates a function PARAMETER — the
# emit≡probe scope-capture regression for fragments, mirroring
# element-literals/func-local.txtar. A naive probe that hoisted the
# fragment into a separate top-level func would not see `name` and would
# false-`undefined`. analyze.go must lower the embedded fragment to an
# inline scope-capturing IIFE (mirroring emit's inline gsx.Func closure) so
# the interpolation resolves against the surrounding lexical scope. Same
# import/leading-component layout as return.txtar.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component Noop() {
	<span/>
}

func Greeting(name string) gsx.Node {
	return <><span>Hello </span><span>{ name }</span></>
}

component Host() {
	{ Greeting("world") }
}
-- invoke --
Host()
-- diagnostics.golden --
-- render.golden --
<span>Hello </span><span>world</span>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
)

//line input.gsx:5:1
func Noop() gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:6:2
		_gsxgw.S("<span/>")
		return _gsxgw.Err()
	})
}

func Greeting(name string) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:10:11
		_gsxgw.S("<span>Hello </span>")
//line input.gsx:10:30
		_gsxgw.S("<span>")
//line input.gsx:10:36
		_gsxgw.Text(string(name))
		_gsxgw.S("</span>")
		return _gsxgw.Err()
	})
}

//line input.gsx:13:1
func Host() gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:14:2
		_gsxgw.Node(ctx, Greeting("world"))
		return _gsxgw.Err()
	})
}
