-- model.go --
package views

type tag struct{ name string }

func (t tag) String() string { return "#" + t.name }

-- input.gsx --
package views

import "github.com/gsxhq/gsx"

component Label(text gsx.Node) {
	<span>{text}</span>
}

component Page(t tag) {
	<Label text={t}/>
}
-- invoke --
Page(tag{name: "go"})
-- diagnostics.golden --
10:2: attribute "text" cannot be passed to github.com/gsxhq/gsx.Node: zero.go:2:18: cannot use _gsxvalue (variable of struct type views.tag) as _gsxwant value in variable declaration: views.tag does not implement gsx.Node (missing method Render)
-- render.golden --
-- generated.x.go.golden --
