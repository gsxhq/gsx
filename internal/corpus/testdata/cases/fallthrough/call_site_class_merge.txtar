-- input.gsx --
package views

component Button(variant string) {
	<button class={ "btn", variant }>{children}</button>
}

component Page() {
	<Button variant="primary" class="w-full">Go</Button>
}
-- invoke --
Page(PageProps{})
-- diagnostics.golden --
-- render.golden --
<button class="btn primary w-full">Go</button>
