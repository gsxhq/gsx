-- input.gsx --
package views

component Box(active bool) {
	<button { if active { id="real" } }>x</button>
}

component Page() {
	<Box active={true} id="caller"/>
}
-- diagnostics.golden --
8:21: attribute on <Box> matches no declared prop of component Box, and its body does not reference `attrs` (no fallthrough bag); reference `attrs` in <Box>'s body to accept forwarded attributes
