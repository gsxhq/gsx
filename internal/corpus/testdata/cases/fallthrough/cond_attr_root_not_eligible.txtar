-- input.gsx --
package views

component Box(active bool) {
	<button { if active { id="real" } }>x</button>
}

component Page() {
	<Box active={true} id="caller"/>
}
-- diagnostics.golden --
8:21: Box has no attrs param; unexpected attribute "id"
