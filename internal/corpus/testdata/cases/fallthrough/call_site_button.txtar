-- input.gsx --
package views

component Button(variant string) {
	<button class="btn" data-variant={variant}>{children}</button>
}

component Page() {
	<Button variant="primary" class="w-full" data-test="x" hx-post="/go">Save</Button>
}
-- diagnostics.golden --
8:28: attribute on <Button> matches no declared prop of component Button, and its body does not reference `attrs` (no fallthrough bag); reference `attrs` in <Button>'s body to accept forwarded attributes
