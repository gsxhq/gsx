# A lowercase package variable holding a Node value is not callable, so its tag
# remains a leaf element rather than becoming a component invocation.
-- input.gsx --
package views

var chip = <em class="chip">c</em>

component Page() {
	<chip/>
}
-- invoke --
Page()
-- diagnostics.golden --
-- render.golden --
<chip/>
