-- input.gsx --
package views

component Status(n int) {
	<p>{ if n > 0 { <span>pos</span> } else if n < 0 { <span>neg</span> } else { <span>zero</span> } }</p>
}
-- invoke --
Status(-1)
-- diagnostics.golden --
-- render.golden --
<p><span>neg</span></p>
