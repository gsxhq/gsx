# NEGATIVE CONTROL: a cond-attr class with NO spread on the element (no
# forwarding at all) must NOT trigger elementFolds (first == nil in
# firstTwoSpreadAttrs) — it stays on the plain per-branch static emission path,
# not the fold/ConcatAttrs/AttrsCond machinery. Pins the generated shape so a
# future over-eager trigger extension is caught here.
-- input.gsx --
package views

component Box(a bool) {
	<div { if a { class="on" } }>x</div>
	<div class="base" { if a { style="color:red" } }>y</div>
}
-- invoke --
gsx.Fragment(
	Box(BoxProps{A: true}),
	Box(BoxProps{A: false}),
)
-- diagnostics.golden --
-- render.golden --
<div class="on">x</div><div class="base" style="color:red">y</div><div>x</div><div class="base">y</div>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	_gsxctx "context"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

type BoxProps struct {
	A bool
}

//line input.gsx:3:1
func Box(_gsxp BoxProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		a := _gsxp.A
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<div")
		if a {
			_gsxgw.S(" class=\"on\"")
		}
		_gsxgw.S(">x</div>")
//line input.gsx:5:2
		_gsxgw.S("<div class=\"base\"")
		if a {
			_gsxgw.S(" style=\"color:red\"")
		}
		_gsxgw.S(">y</div>")
		return _gsxgw.Err()
	})
}
