# Context: an f`…@{expr}…` VALUE literal whose hole references a FUNC-LOCAL
# name — the parameter `who` of a top-level helper func (`func makeGreeting(who
# string) string { return f`hi @{who}` }`). The literal rides in the top-level
# GoWithElements split; its hole must resolve against the enclosing lexical
# scope (the func param), NOT a hoisted-out scope, so this MUST generate with
# empty diagnostics. Confirms the probe splices a scope-capturing IIFE the same
# way the embedded-element path does.
-- input.gsx --
package demo

func makeGreeting(who string) string {
	return f`hi @{who}`
}

component Greeting(who string) {
	<p>{ makeGreeting(who) }</p>
}
-- invoke --
Greeting(GreetingProps{Who: "Ada"})
-- diagnostics.golden --
-- render.golden --
<p>hi Ada</p>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package demo

import (
	_gsxctx "context"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

func makeGreeting(who string) string {
	return "hi " + string(who)
}

type GreetingProps struct {
	Who string
}

//line input.gsx:7:1
func Greeting(_gsxp GreetingProps) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		who := _gsxp.Who
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:8:2
		_gsxgw.S("<p>")
//line input.gsx:8:5
		_gsxgw.Text(string(makeGreeting(who)))
		_gsxgw.S("</p>")
		return _gsxgw.Err()
	})
}
