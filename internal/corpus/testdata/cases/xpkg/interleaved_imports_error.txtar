-- input.gsx --
package views

func helper() string { return "x" }

import "fmt"

var _ = fmt.Sprintf

component Bad() {
	<p>{helper()}</p>
}
-- diagnostics.golden --
invalid Go in pass-through block: 6:1: imports must appear before other declarations
