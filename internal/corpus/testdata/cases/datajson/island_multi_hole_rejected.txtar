-- input.gsx --
package views

component Page(a string, b string) {
	<script type="application/json">@{ a } @{ b }</script>
}
-- diagnostics.golden --
4:2: jsx: a data <script> must contain exactly one @{ } value; found more than one
