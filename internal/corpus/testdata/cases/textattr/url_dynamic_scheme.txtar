# A dynamic scheme now simply works when it is safe: the whole value is assembled
# ("https" + "://ex.com") and sanitized -> https is allow-listed -> passes through.
# (An unsafe dynamic scheme would be blocked, see url_dangerous_blocked.)
-- input.gsx --
package p

component Link(scheme string) {
	<a href=f`@{scheme}://ex.com`>go</a>
}
-- invoke --
Link(LinkProps{Scheme: "https"})
-- diagnostics.golden --
-- render.golden --
<a href="https://ex.com">go</a>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package p

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
)

type LinkProps struct {
	Scheme string
}

//line input.gsx:3:1
func Link(_gsxp LinkProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		scheme := _gsxp.Scheme
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<a href=\"")
		_gsxgw.URL(string(scheme) + "://ex.com")
		_gsxgw.S("\">go</a>")
		return _gsxgw.Err()
	})
}
