# SECURITY: a URL-context whole-literal pipeline sanitizes the PIPE's OUTPUT,
# not its input. A filter (upper, here) that turns a safe-looking hole into a
# dangerous scheme is still blocked — _gsxgw.URL() runs on the LOWERED
# (post-pipe) expression, never on the raw assembled string.
-- input.gsx --
package p

component Link(u string) {
	<a href={f`@{u}` |> upper}>go</a>
}
-- invoke --
Link("javascript:alert(1)")
-- diagnostics.golden --
-- render.golden --
<a href="about:invalid#gsx">go</a>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package p

import (
	_gsxctx "context"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxstd "github.com/gsxhq/gsx/std"
	_gsxio "io"
)

//line input.gsx:3:1
func Link(u string) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<a href=\"")
		_gsxgw.URL(string(_gsxstd.Upper((string(u)))))
		_gsxgw.S("\">go</a>")
		return _gsxgw.Err()
	})
}
