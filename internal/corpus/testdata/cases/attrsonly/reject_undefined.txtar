# A tag with no declaration anywhere in the package still hits the probe's own
# positioned `undefined: NopeIcon` — the attrs-only gate must not make a
# missing identifier worse (no new diagnostic code, no swallowed position).
-- input.gsx --
package views

component Page() {
	<NopeIcon class="x"/>
}
-- diagnostics.golden --
4:14: undefined: NopeIcon
