# (T, error) auto-unwrap: single tuple-valued ordered-attrs pair.
# sig returns (string, error); codegen hoists it before the Card invocation.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

func sig(name string) (string, error) { return "{" + name + ":0}", nil }

component Card(bag gsx.OrderedAttrs) {
	<div { bag... }>x</div>
}

component Page(t string) {
	<Card bag={{ "data-signals": sig(t) }}/>
}
-- invoke --
Page(PageProps{T: "count"})
-- diagnostics.golden --
7:10: undefined: gsx.OrderedAttrs
-- generated.x.go.golden --
-- render.golden --
