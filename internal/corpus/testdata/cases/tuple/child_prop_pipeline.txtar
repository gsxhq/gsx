# (T, error) auto-unwrap: pipeline in a child-component prop position.
# The pipeline `name |> upper` lowers to `_gsxstd0.Upper(name)` and returns a
# plain string (not a tuple), so no hoisting occurs — verifying non-tuple pipeline
# props continue to emit unchanged.
# Note: a pipeline filter returning (R, error) would require corpus infrastructure
# to register a custom filter package; that case is deferred to Task 4 infra work.
-- input.gsx --
package views

component Label(text string) { <span>{text}</span> }

component Page(name string) { <Label text={ name |> upper }/> }
-- invoke --
Page(PageProps{Name: "ada"})
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
	_gsxstd "github.com/gsxhq/gsx/std"
)

type LabelProps struct {
	Text  string
	Attrs gsx.Attrs
}

//line input.gsx:3:1
func Label(_gsxp LabelProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		text := _gsxp.Text
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:3:32
		_gsxgw.S("<span")
		_gsxgw.ClassMerged(gsx.DefaultClassMerge, _gsxp.Attrs.Class())
		_gsxgw.StyleMerged("", _gsxp.Attrs.Style())
		_gsxgw.Spread(ctx, _gsxp.Attrs.Without("class", "style"))
		_gsxgw.S(">")
//line input.gsx:3:38
		_gsxgw.Text(string(text))
		_gsxgw.S("</span>")
		return _gsxgw.Err()
	})
}

type PageProps struct {
	Name string
}

//line input.gsx:5:1
func Page(_gsxp PageProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		name := _gsxp.Name
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:5:31
		_gsxgw.Node(ctx, Label(LabelProps{Text: _gsxstd.Upper((name))}))
		return _gsxgw.Err()
	})
}
-- render.golden --
<span>ADA</span>
