# (T, error) auto-unwrap: whitespace-around-= variant of ordered-attrs.
# Ensures the whitespace-around-= parse path also hoists tuple pair values.
-- input.gsx --
package views

import "github.com/gsxhq/gsx"

func sig(name string) (string, error) { return "{" + name + ":0}", nil }

component Card(bag gsx.OrderedAttrs) {
	<div { bag... }>y</div>
}

component Page(t string) {
	<Card bag = {{ "data-signals": sig(t) }}/>
}
-- invoke --
Page(PageProps{T: "c"})
-- diagnostics.golden --
7:10: undefined: gsx.OrderedAttrs
-- generated.x.go.golden --
-- render.golden --
