# (T, error) auto-unwrap regression guard: untyped-literal child props bound to
# NON-default-typed fields must still generate. Only a function CALL can yield a
# (T, error) tuple, so literals/idents must NOT be wrapped in _gsxunwrap (which
# would fix an untyped constant to its DEFAULT type and break assignability,
# e.g. `cannot use 100 (value of type int) as float64`).
-- input.gsx --
package views

type Count int

type Toggle bool

component Gauge(value float64, n int8, c Count, on Toggle) { <div>{value}</div> }

component Page() { <Gauge value={100} n={3} c={5} on={true}/> }
-- invoke --
Page()
-- diagnostics.golden --
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package views

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
)

type Count int

type Toggle bool

type GaugeProps struct {
	Value float64
	N     int8
	C     Count
	On    Toggle
}

//line input.gsx:7:1
func Gauge(_gsxp GaugeProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		value := _gsxp.Value
		_gsxgw := gsx.W(_gsxw)
		var _gsxnum [32]byte
//line input.gsx:7:62
		_gsxgw.S("<div>")
//line input.gsx:7:67
		_gsxgw.FloatInto(_gsxnum[:], float64(value))
		_gsxgw.S("</div>")
		return _gsxgw.Err()
	})
}

//line input.gsx:9:1
func Page() gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:9:20
		_gsxgw.Node(ctx, Gauge(GaugeProps{Value: 100, N: 3, C: 5, On: true}))
		return _gsxgw.Err()
	})
}
-- render.golden --
<div>100</div>
