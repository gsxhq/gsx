# W3c Step 1: a nested js`` literal in a BODY-position f`` literal's @{ } hole
# — pins EscapeJSVal (the js`` literal's own per-hole escaping, wrapping the
# result in RawJS) composing correctly with the OUTER body-text HTML escaping:
# `who`'s hostile bytes must be JS-escaped by the inner literal, and the
# resulting (safe, mostly-ASCII) text must then ALSO survive the outer
# element's Text() HTML-escaping unchanged.
-- input.gsx --
package p

component Row(who string) {
	<p>{f`a @{ string(js`f(@{who})`) }`}</p>
}
-- invoke --
Row("<b>\"'")
-- diagnostics.golden --
-- render.golden --
<p>a f(&#34;\u003cb\u003e\&#34;&#39;&#34;)</p>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package p

import (
	_gsxctx "context"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

//line input.gsx:3:1
func Row(who string) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<p>")
//line input.gsx:4:5
		_gsxgw.S("a ")
//line input.gsx:4:11
		_gsxv0 := _gsxrt.EscapeJSVal(who)
		_gsxgw.Text(string(string(_gsxrt.RawJS("f(" + _gsxv0 + ")"))))
		_gsxgw.S("</p>")
		return _gsxgw.Err()
	})
}
