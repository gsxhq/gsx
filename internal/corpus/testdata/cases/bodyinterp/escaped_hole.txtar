# ESCAPING: inside a body backtick literal, `\@{...}` is an escaped hole — it
# emits the literal text `@{lit}` (verbatim static, no interpolation), while the
# unescaped `@{id}` is a real hole rendered in Text context.
-- input.gsx --
package p

component Row(id string) {
	<p>{f`a-\@{lit}-@{id}`}</p>
}
-- invoke --
Row(RowProps{Id: "a&b"})
-- diagnostics.golden --
-- render.golden --
<p>a-@{lit}-a&amp;b</p>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package p

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
)

type RowProps struct {
	Id string
}

//line input.gsx:3:1
func Row(_gsxp RowProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		id := _gsxp.Id
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<p>")
//line input.gsx:4:5
		_gsxgw.S("a-@{lit}-")
//line input.gsx:4:19
		_gsxgw.Text(string(id))
		_gsxgw.S("</p>")
		return _gsxgw.Err()
	})
}
