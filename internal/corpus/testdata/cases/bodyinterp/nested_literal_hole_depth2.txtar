# W3c Step 1: a nested f`` literal that ITSELF has a hole (@{who}) referencing
# a hostile prop value — pins that HTML-escaping still applies exactly once at
# the OUTER body-text sink, regardless of nesting depth: the inner literal is a
# pure string concat (no escaping of its own), so `who`'s hostile bytes reach
# the outer element's Text() sink unescaped and get escaped there.
-- input.gsx --
package p

component Row(who string) {
	<p>{f`a @{ f`b @{who}` } c`}</p>
}
-- invoke --
Row("<b>")
-- diagnostics.golden --
-- render.golden --
<p>a b &lt;b&gt; c</p>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package p

import (
	_gsxctx "context"
	_gsxrt "github.com/gsxhq/gsx"
	_gsxio "io"
)

//line input.gsx:3:1
func Row(who string) _gsxrt.Node {
	return _gsxrt.Func(func(ctx _gsxctx.Context, _gsxw _gsxio.Writer) error {
		_gsxgw := _gsxrt.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<p>")
//line input.gsx:4:5
		_gsxgw.S("a ")
//line input.gsx:4:11
		_gsxgw.Text(string("b " + string(who)))
		_gsxgw.S(" c</p>")
		return _gsxgw.Err()
	})
}
