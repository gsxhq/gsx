# body backtick literal with a whole-literal pipeline: the segments assemble
# into ONE Go string expression, piped through the filter, then rendered for
# Text context — not per-segment.
-- input.gsx --
package p

component Item(id string) {
	<p>{f`item-@{id}` |> upper}</p>
}
-- invoke --
Item(ItemProps{Id: "a&b"})
-- diagnostics.golden --
-- render.golden --
<p>ITEM-A&amp;B</p>
-- generated.x.go.golden --
// Code generated by gsx; DO NOT EDIT.

package p

import (
	"context"
	"io"

	"github.com/gsxhq/gsx"
	_gsxstd "github.com/gsxhq/gsx/std"
)

type ItemProps struct {
	Id string
}

//line input.gsx:3:1
func Item(_gsxp ItemProps) gsx.Node {
	return gsx.Func(func(ctx context.Context, _gsxw io.Writer) error {
		id := _gsxp.Id
		_gsxgw := gsx.W(_gsxw)
//line input.gsx:4:2
		_gsxgw.S("<p>")
//line input.gsx:4:5
		_gsxgw.Text(string(_gsxstd.Upper(("item-" + string(id)))))
		_gsxgw.S("</p>")
		return _gsxgw.Err()
	})
}
