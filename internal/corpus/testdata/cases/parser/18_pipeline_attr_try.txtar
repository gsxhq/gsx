gsx pipeline: attribute value with a failable filter stage.
-- input.gsx --
package examples

component C(u string) { <a href={ u |> absolute()? }>x</a> }
-- diagnostics.golden --
3:33: the `?` try-marker is not supported; gsx auto-unwraps (T, error) values — remove the `?` (handle the error explicitly with `{ if v, err := f(); err != nil { … } }`)
-- ast.golden --
File package=examples
