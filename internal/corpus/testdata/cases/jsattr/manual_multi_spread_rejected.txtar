-- input.gsx --
package views

component Box() {
	<div {...attrs} id="x" {...attrs}>{children}</div>
}
-- diagnostics.golden --
cases/jsattr_manual_multi_spread_rejected/input.gsx: codegen: more than one {...attrs} spread on an element; precedence is ambiguous
