-- input.gsx --
package views

type State struct {
	Tab  string
	Open bool
}

component Page(state State) {
	<div x-data={ state }>x</div>
}
-- invoke --
Page(State{Tab: "home", Open: false})
-- diagnostics.golden --
9:7: attribute value type corpustest/cases/jsattr_xdata_whole_value.State not supported (string/number/bool/Stringer only)
-- render.golden --
-- generated.x.go.golden --
