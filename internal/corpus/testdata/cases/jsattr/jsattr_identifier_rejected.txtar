-- input.gsx --
package views

component Page() {
	<div x-on:click="@{ stmt } = 1">x</div>
}
-- diagnostics.golden --
4:20: jsx: @{ } here is not a safe JavaScript value position (it looks like an identifier/binding); wrap the value where it is used as a value, or use a data attribute
