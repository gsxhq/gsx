# error: unterminated { in child content swallows the close tag, so the element is unterminated
-- input.gsx --
package p

component X() { <p>{ x </p> }
-- ast.golden --
-- diagnostics.golden --
unexpected EOF, expected </p>
